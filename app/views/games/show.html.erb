<p id="notice"><%= notice %></p>

<div>
Name: <%= @game.name %>
Score Type : <%= @game.scoreType %>
Fixed Amount : <%= @game.fixedAmount %>
Score Cap : <%= @game.scoreCap %>

</div>

<div>
Scores:
<%= render @game.scores %>	
</div>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
